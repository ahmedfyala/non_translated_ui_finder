name: non_translated_ui_finder
description: A Dart package to scan your project for hard-coded UI texts and generate a clickable HTML report.
version: 1.0.0
repository: https://github.com/ahmedfyala/non_translated_ui_finder
environment:
  sdk: '>=2.18.0 <3.0.0'

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  path: ^1.9.1
dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
